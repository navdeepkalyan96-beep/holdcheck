services:
  - type: web
    name: holdcheck
    runtime: python
    plan: free
    rootDir: services/pricing
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
